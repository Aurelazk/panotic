# Copier ce fichier : cp .env.example .env
# Puis lancer la base : docker compose up -d db

DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=panotic_user
DB_PASSWORD=panotic_pass123
DB_DATABASE=panotic

JWT_SECRET=changez_moi_en_production

PORT=3000
